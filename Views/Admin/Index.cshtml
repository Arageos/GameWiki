@model IEnumerable<GameWiki.DTOs.Admin.UserListDto>
@{
    ViewData["Title"] = "Panel Zarządzania";
    var currentUserId = int.Parse(User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)!.Value);
}

<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-white fw-bold">Panel Administracyjny 🛡️</h2>
        <a asp-controller="Admin" asp-action="PendingArticles" class="btn btn-outline-warning">
            📋 Artykuły do weryfikacji
        </a>
        <a asp-controller="Admin" asp-action="PendingReviews" class="btn btn-outline-info">
            ⭐ Recenzje do weryfikacji
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="gw-card p-4" style="background: #1a1d26; border-radius: 15px; border: 1px solid rgba(255,255,255,0.1);">
        <div class="table-responsive">
            <table class="table table-dark table-hover align-middle mb-0" style="background: transparent;">
                <thead>
                    <tr style="border-bottom: 2px solid #334155;">
                        <th class="text-uppercase small" style="color: #94a3b8;">Użytkownik</th>
                        <th class="text-uppercase small" style="color: #94a3b8;">Email</th>
                        <th class="text-uppercase small" style="color: #94a3b8;">Rola</th>
                        <th class="text-uppercase small" style="color: #94a3b8;">Status</th>
                        <th class="text-uppercase small text-end" style="color: #94a3b8;">Akcje</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model)
                    {
                        <tr style="border-bottom: 1px solid rgba(255,255,255,0.05);">
                            <td class="fw-bold text-white py-3">@user.Username</td>
                            <td class="text-light">@user.Email</td>
                            <td>
                                @if (user.RoleName == "Admin")
                                {
                                    <span class="badge bg-danger">Admin</span>
                                }
                                else if (user.RoleName == "Moderator")
                                {
                                    <span class="badge bg-warning text-dark">Moderator</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">User</span>
                                }
                            </td>
                            <td>
                                @if (user.IsBanned)
                                {
                                    <span class="badge bg-danger">Zbanowany</span>
                                }
                                else
                                {
                                    <span class="badge bg-success">Aktywny</span>
                                }
                            </td>
                            <td class="text-end">
                                @if (user.Id != currentUserId && user.RoleName != "Admin")
                                {
                                    <div class="d-flex justify-content-end gap-2">
                                        @if (user.RoleName == "Moderator")
                                        {
                                            @if (User.IsInRole("Admin"))
                                            {
                                                <form asp-action="ToggleModRole" method="post" class="m-0">
                                                    <input type="hidden" name="userId" value="@user.Id" />
                                                    <button type="submit" class="btn btn-sm btn-outline-warning">
                                                        Zdegraduj z Moda
                                                    </button>
                                                </form>
                                            }
                                        }
                                        else
                                        {
                                            @if (User.IsInRole("Admin") || User.IsInRole("Moderator"))
                                            {
                                                @if (user.IsBanned)
                                                {
                                                    <form asp-action="ToggleBan" method="post" class="m-0">
                                                        <input type="hidden" name="userId" value="@user.Id" />
                                                        <button type="submit" class="btn btn-sm btn-outline-success">Odbanuj</button>
                                                    </form>
                                                }
                                                else
                                                {
                                                    <button type="button" class="btn btn-sm btn-outline-danger"
                                                            data-bs-toggle="modal" data-bs-target="#banModal-@user.Id">
                                                        Zbanuj
                                                    </button>

                                                    <div class="modal fade" id="banModal-@user.Id" tabindex="-1">
                                                        <div class="modal-dialog modal-dialog-centered">
                                                            <div class="modal-content" style="background: #1a1d26; border: 1px solid rgba(255,255,255,0.1);">
                                                                <div class="modal-header border-secondary">
                                                                    <h5 class="modal-title text-white">Zbanuj użytkownika @user.Username</h5>
                                                                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                                                </div>
                                                                <form asp-action="ToggleBan" method="post">
                                                                    <div class="modal-body">
                                                                        <input type="hidden" name="userId" value="@user.Id" />
                                                                        <label class="form-label text-light">Powód bana (wyświetlony przy logowaniu)</label>
                                                                        <textarea name="banReason" class="form-control" rows="3"
                                                              style="background: #0f1117; color: #e2e8f0; border-color: #334155;"
                                                              placeholder="np. Spam, wulgaryzmy, wielokrotne naruszenia regulaminu..."></textarea>
                                                                    </div>
                                                                    <div class="modal-footer border-secondary">
                                                                        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Anuluj</button>
                                                                        <button type="submit" class="btn btn-danger">Zbanuj</button>
                                                                    </div>
                                                                </form>
                                                            </div>
                                                        </div>
                                                    </div>
                                                }
                                            }

                                            @if (User.IsInRole("Admin") && !user.IsBanned)
                                            {
                                                <form asp-action="ToggleModRole" method="post" class="m-0">
                                                    <input type="hidden" name="userId" value="@user.Id" />
                                                    <button type="submit" class="btn btn-sm btn-outline-info">
                                                        Awansuj na Moda
                                                    </button>
                                                </form>
                                            }
                                        }
                                    </div>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
